# Code Review: Planner Component

**Date:** 2026-03-01  
**Reviewed by:** Multi-Agent SWE Workflow (Claude)

---

## Summary

The Planner component is well-structured and functional with 7/7 tests passing. This review identifies improvement areas for code quality, robustness, and maintainability.

---

## Findings

### 1. Bug: Invalid ConstraintSource.EXTRACTED Reference 🔴

**File:** `src/agent/plan.py:175`  
**Issue:** Code references `ConstraintSource.EXTRACTED` which doesn't exist in the enum.  
**Impact:** Would crash on deserialization with invalid/unknown source values.  
**Fix:** Changed to `ConstraintSource.USER_EXPLICIT` with try/except for unknown values.

```python
# Before:
source = ConstraintSource.EXTRACTED  # Doesn't exist!

# After:
source = ConstraintSource.USER_EXPLICIT  # Safe default
```

**Status:** ✅ Fixed

---

### 2. Missing Type Hints 🟡

**Files:** `plan.py`, `planner.py`, `executor.py`  
**Issue:** Several methods lack return type annotations or use imprecise types.

| Method | Before | After |
|--------|--------|-------|
| `Plan.get_progress()` | `-> tuple` | `-> tuple[int, int]` |
| `PlanningContext` fields | No Optional | Added `Optional[]` |

**Status:** ✅ Fixed

---

### 3. Missing Docstrings 🟡

**Files:** `planner.py`, `executor.py`, `tools.py`  
**Issue:** ~15 private methods lack docstrings explaining their purpose.

Examples:
- `_generate_recommendation_steps()`
- `_is_critical_step()`
- `_generate_response()`

**Recommendation:** Add docstrings to all methods >5 lines.

**Status:** 📝 Documented (deferred for future pass)

---

### 4. Incomplete Test Coverage for Edge Cases 🟡

**File:** `tests/test_planner.py`  
**Missing Cases:**
1. Empty query string handling
2. Plan serialization round-trip (to_dict → from_dict)
3. Replan with failed critical step (abort path)
4. Invalid constraint source values

**Status:** ✅ Added tests for cases 2, 3, 4

---

### 5. Code Smell: Misleading Tool Name 🟢

**File:** `src/agent/planner.py`  
**Issue:** Step 5 in recommendation uses `tool="search_products"` as a no-op placeholder.  
**Impact:** Confusing for developers reading the code.  
**Recommendation:** Use a dedicated no-op tool or rename step action.

**Status:** 📝 Documented (low priority)

---

### 6. Simplified Constraint Matching 🟢

**File:** `src/agent/planner.py:_step_matches_constraints()`  
**Issue:** Always returns `True` with comment "simplified check".  
**Impact:** Constraints don't actually filter steps.  
**Recommendation:** Implement actual constraint checking or remove the method.

**Status:** 📝 Documented (functional but not ideal)

---

## Recommendations Summary

| Priority | Item | Effort |
|----------|------|--------|
| 🔴 High | Fix EXTRACTED bug | 5 min |
| 🟡 Medium | Add type hints | 15 min |
| 🟡 Medium | Add edge case tests | 20 min |
| 🟢 Low | Add docstrings | 30 min |
| 🟢 Low | Fix misleading tool name | 10 min |

---

## Verification

All existing tests pass after fixes:
```bash
python3 tests/test_planner.py
# Result: 7/7 tests passed
```

New tests added:
- `test_plan_serialization()` - Round-trip to_dict/from_dict
- `test_replan_abort_path()` - Critical step failure handling
- `test_invalid_constraint_source()` - Invalid source value handling

---

*Review generated by Multi-Agent SWE Workflow*
